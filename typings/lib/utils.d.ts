/**
 *创建基本引擎工具函数
 * @param {Provider} 提供器实例
 * @return {Engine} 构建的引擎实例
 */
export function createEngine(provider?: Provider): Engine;
export const MODULE_KEY: string;
import { Provider } from "./provider.js";
import { Engine } from "./engine.js";
