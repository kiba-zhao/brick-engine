/**
 * 创建config对象函数
 * @param {Array<Module>} entries config入口
 * @param {String} env 配置环境变量
 * @param {Object} ctx 上下文环境对象
 * @return {Object} config配置对象
 */
export function createConfig(entries: Array<any>, env: string, ctx: any): any;
